pymongo<4.8
flask
flask-restx
flask-expects-json
requests
flask-cors
escpos
pillow
pyyaml
cffi
PyJWT
flask_limiter
fpdf2
gunicorn<22
python-dotenv
get-docker-secret
flask_monitoringdashboard
flask-socketio
bcrypt
# eventlet: optional. Nur für die Container-Variante (gunicorn eventlet-Worker).
# Auf dem Pi darf der Build fehlschlagen — der Code fällt dann auf 'threading' zurück.
# Bewusst als letzte Zeile, damit ein Fehler die anderen Pakete nicht blockiert.
eventlet